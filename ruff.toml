line-length = 100
target-version = "py310"

[lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]  # long lines (URLs, prompt strings) are fine

[lint.per-file-ignores]
"tests/*" = ["E402"]  # tests import tools after conftest sets sys.path
