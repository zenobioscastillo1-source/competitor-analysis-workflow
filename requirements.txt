# Core
python-dotenv>=1.0

# LLM (Google Gemini — free tier available)
google-genai>=2.6

# Web scraping
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0

# Headless-browser rendering: JS-heavy scrapes AND branded PDF export.
# After install, run once: playwright install chromium
playwright>=1.40

# Branded PDF report templating (tools/render_pdf_report.py)
jinja2>=3.1

# Google Workspace (Sheets / Docs / Slides / Drive)
google-api-python-client>=2.100
google-auth>=2.23
google-auth-oauthlib>=1.1
google-auth-httplib2>=0.2

# Slack
slack_sdk>=3.23
